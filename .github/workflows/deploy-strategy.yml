name: Strategy Service CI/CD

on:
  push:
    branches: [ "develop" ]

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: pi-strategy
  SERVICE_NAME: planit-strategy-svc

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 1. Checkout Code
      uses: actions/checkout@v3

    - name: 2. Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'corretto'

    - name: 3. Build with Gradle
      id: build
      continue-on-error: true
      run: |
        chmod +x gradlew
        ./gradlew clean build -x test

    - name: 4. Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: 5. Login to Amazon ECR
      id: login-ecr
      continue-on-error: true
      uses: aws-actions/amazon-ecr-login@v1

    - name: 6. Build and push Docker image
      id: docker
      continue-on-error: true
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        IMAGE_TAG: ${{ github.sha }}
      run: |
        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

    - name: 7. Checkout Manifest Repository
      uses: actions/checkout@v3
      with:
        repository: barumi-acs/Planit-manifests # ◀── 매니페스트 레포 주소
        token: ${{ secrets.GH_PAT }}           # ◀── 미리 등록한 PAT (Personal Access Token)
        ref: develop                           # ◀── develop 브랜치 사용
        path: manifest-repo                    # ◀── 현재 작업 폴더 내 'manifest-repo' 폴더에 저장

    # 8. deployment.yaml 파일 내 이미지 태그 수정
    - name: 8. Update Image Tag in YAML
      id: manifest
      continue-on-error: true
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        ECR_REPOSITORY: pi-strategy              # ◀── Strategy 서비스용 ECR 레포 명칭 확인!
        IMAGE_TAG: ${{ github.sha }}
      run: |
        TARGET_FILE="manifest-repo/strategy-svc/deployment.yaml"
        FULL_IMAGE="$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"

        sed -i "s|image: .*|image: $FULL_IMAGE|g" $TARGET_FILE

        cat $TARGET_FILE | grep "image:"

    # 9. 변경사항 커밋 및 푸시
    - name: 9. Push Changes to Manifest Repo
      id: push
      continue-on-error: true
      run: |
        cd manifest-repo
        git config --global user.email "action@github.com"
        git config --global user.name "GitHub Action Bot"
        
        git add .

        if git diff --cached --quiet; then
          echo "No changes to commit"
        else
          git commit -m "chore(strategy-svc): update image to ${{ github.sha }}"
          git push origin develop
        fi
    
    - name: Configure AWS for Notification
      if: always()
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Detect Failed Step
      if: always()
      run: |
        if [ "${{ steps.build.outcome }}" = "failure" ]; then
          echo "FAILED_STAGE=build" >> $GITHUB_ENV
        elif [ "${{ steps.login-ecr.outcome }}" = "failure" ]; then
          echo "FAILED_STAGE=ecr-login" >> $GITHUB_ENV
        elif [ "${{ steps.docker.outcome }}" = "failure" ]; then
          echo "FAILED_STAGE=docker" >> $GITHUB_ENV
        elif [ "${{ steps.manifest.outcome }}" = "failure" ]; then
          echo "FAILED_STAGE=deploy" >> $GITHUB_ENV
        elif [ "${{ steps.push.outcome }}" = "failure" ]; then
          echo "FAILED_STAGE=git-push" >> $GITHUB_ENV
        else
          echo "FAILED_STAGE=none" >> $GITHUB_ENV
        fi

      # 🔥 실패 알림
    - name: 🔥 Notify CI Failure
      if: always() && failure()
      run: |
        aws sns publish \
          --region ${{ env.AWS_REGION }} \
          --topic-arn ${{ secrets.SNS_TOPIC_ARN }} \
          --message '{
            "type": "CI",
            "workflow": "${{ github.workflow }}",
            "service": "'$SERVICE_NAME'",
            "env": "dev",
            "stage": "${{ env.FAILED_STAGE }}",
            "status": "FAILED",
            "message": "ERROR at ${{ env.FAILED_STAGE }}",
            "author": "${{ github.actor }}",
            "url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          }'

      # 🚀 성공 알림
    - name: 🚀 Notify CI Success
      if: success()
      run: |
        aws sns publish \
          --region ${{ env.AWS_REGION }} \
          --topic-arn ${{ secrets.SNS_TOPIC_ARN }} \
          --message '{
            "type": "CI",
            "workflow": "${{ github.workflow }}",
            "service": "'$SERVICE_NAME'",
            "env": "dev",
            "stage": "pipeline",
            "status": "SUCCESS",
            "message": "${{ github.event.head_commit.message }}",
            "author": "${{ github.actor }}",
            "url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          }'